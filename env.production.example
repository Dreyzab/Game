# Production Environment Configuration
# Rename this file to .env.production before building for production locally.
# For CI/CD, set these values as GitHub Secrets.

# Backend URL (Update this after deploying the backend to Cloud Run)
VITE_API_URL=https://YOUR_CLOUD_RUN_SERVICE_URL.a.run.app

# Auth (Clerk) - Should match your production Clerk instance
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# Mapbox
VITE_MAPBOX_TOKEN=pk.eyJ1...

# Admin (backend)
# NOTE: This value will be available to the backend only if you also set it in the backend environment.
ADMIN_TOKEN=13121998qwer