# АРХИТЕКТУРА СТРАНИЦЫ ИНВЕНТАРЯ

> ⚠️ **Примечание:** Backend мигрирован с Convex на Bun + Elysia. API эндпоинты см. в `server/src/api/routes/inventory.ts`.

## 📋 ОБЗОР

Страница инвентаря (`/inventory`) — это комплексная система управления предметами, экипировкой и хранилищами персонажа. Реализована с использованием React, Zustand для state management, и Elysia API для backend синхронизации.

---

## 🏗️ СТРУКТУРА КОМПОНЕНТОВ

### Главный компонент: `ModernInventoryPage`

**Расположение:** `src/features/inventory/ui/InventoryPage.tsx`

**Функциональность:**
- Оркестрация всех подкомпонентов
- Синхронизация с backend (Elysia API)
- Управление состоянием через Zustand store
- Обработка событий экипировки/снятия предметов
- Модальное окно сравнения предметов

**Layout:**
```
┌─────────────────────────────────────────────────────────┐
│  Grid: 12 колонок                                      │
│  ┌──────────────┬──────────────────────────────────┐  │
│  │ Левая (4)    │ Правая (8)                        │  │
│  │              │                                   │  │
│  │ Character    │ Search Bar                        │  │
│  │ Panel        │                                   │  │
│  │              │ Inventory Grid                    │  │
│  │ Equipment    │                                   │  │
│  │ Slots        │                                   │  │
│  │              │ Item Details                      │  │
│  │ Quick Access │ Panel                             │  │
│  │              │                                   │  │
│  │ Quick Stats  │                                   │  │
│  │              │                                   │  │
│  │ Containers   │                                   │  │
│  └──────────────┴──────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

---

## 📦 ЛЕВАЯ КОЛОНКА (4 колонки)

### 1. Character Panel (`AnimatedCard`)

#### 1.1. EquipmentSlots
**Компонент:** `src/features/inventory/ui/EquipmentSlots/EquipmentSlots.tsx`

**Функциональность:**
- Отображение силуэта персонажа с наложенными слотами экипировки
- Визуализация экипированных предметов
- Интерактивность:
  - **Двойной тап** — снять предмет
  - **Долгое нажатие** — сравнить с текущим
  - **Hover** — подсветка слота при перетаскивании
- Адаптивное растягивание (aspectRatio 2:3)

**Слоты:**
- `helmet` — Шлем (верх, центр)
- `armor` — Броня (центр, центр)
- `primary` — Основное оружие (центр, слева)
- `secondary` — Вторичное оружие (центр, справа)
- `clothing_bottom` — Штаны (низ, центр)
- `backpack` — Рюкзак (низ, справа)
- `rig` — Разгрузка (низ, слева)

**Особенности:**
- Фоновое изображение силуэта (`stalker_silhouette.png`)
- Абсолютное позиционирование слотов по процентам
- Визуальная обратная связь при drag & drop

#### 1.2. Quick Access Bar
**Компонент:** `src/features/inventory/ui/QuickAccessBar/QuickAccessBar.tsx`

**Функциональность:**
- 5 быстрых слотов для быстрого доступа к предметам
- Drag & drop поддержка
- Визуальная индикация заполненности
- Нумерация слотов (#1-#5)

**Использование:**
- Быстрый доступ к расходникам (медикаменты, гранаты)
- Горячие клавиши для предметов в бою

#### 1.3. Quick Stats Panel
**Компонент:** `src/features/inventory/ui/QuickStatsPanel.tsx`

**Отображает:**
- **Damage** — суммарный урон от экипированного оружия
- **Defense** — суммарная защита от экипированной брони
- **Weight** — текущий вес / максимальный вес (кг)

**Расчет:**
- Статистики вычисляются из экипированных предметов
- Обновляются автоматически при изменении экипировки

#### 1.4. Container Panel
**Компонент:** `src/features/inventory/ui/ContainerPanel.tsx`

**Функциональность:**
- Отображение контейнеров от экипированных предметов
- Сетка предметов внутри контейнера
- Индикация заполненности (например, "3/8")
- Клик по предмету для выбора

**Типы контейнеров:**
- **Шлем** — 1×1 (1 карманчик)
- **Разгрузка** — 4×2 (8 карманчиков)
- **Рюкзак** — 4×4 (16 карманчиков)
- **Ремень** — 4×1 (4 карманчика)

**Особенности:**
- Автоматическое создание при экипировке
- Удаление при снятии предмета
- Визуализация пустых слотов

---

## 📦 ПРАВАЯ КОЛОНКА (8 колонок)

### 2. Search Bar
**Компонент:** Встроен в `InventoryPage.tsx`

**Функциональность:**
- Поиск по названию предмета
- Фильтрация в реальном времени
- Подсветка при фокусе (amber border)

### 3. Enhanced Inventory Grid
**Компонент:** `src/features/inventory/ui/EnhancedInventoryGrid.tsx` → `InventoryGrid.tsx`

**Функциональность:**
- **Tetris-подобная сетка** для размещения предметов
- Размеры предметов учитываются (width × height)
- Drag & Drop:
  - Перетаскивание между ячейками сетки
  - Перетаскивание на слоты экипировки
  - Перетаскивание в quick slots
  - Перетаскивание в контейнеры
- Визуальная обратная связь:
  - Подсветка целевой ячейки
  - Drag overlay с превью предмета
  - Tooltip при hover

**Интерактивность:**
- **Одиночный клик** — выбор предмета
- **Двойной клик** — автоматическая экипировка (TODO)
- **Долгое нажатие** — сравнение предметов

**Особенности:**
- Защита квестовых предметов (amber индикатор)
- Визуализация редкости предметов
- Предотвращение перекрытия предметов
- Автоматическое размещение новых предметов

### 4. Inventory Detail Panel
**Компонент:** `src/features/inventory/ui/InventoryDetailPanel.tsx` → `ItemDetailsPanel.tsx`

**Функциональность:**
- Отображение детальной информации о выбранном предмете
- Статистики:
  - Damage (DMG)
  - Defense (DEF)
  - Weight (WT)
  - Size (SIZE) — размер в сетке
  - Condition (COND) — состояние/прочность
- Теги предмета
- Редкость (цветовая кодировка)

**Действия:**
- **Equip/Unequip** — экипировка/снятие
- **Inspect** — детальный осмотр (TODO)

**Placeholder:**
- Показывает подсказку при отсутствии выбранного предмета

---

## 🔄 МОДАЛЬНЫЕ ОКНА

### 5. Item Comparison Modal
**Компонент:** `src/features/inventory/ui/ItemComparison/ItemComparison.tsx`

**Функциональность:**
- Сравнение выбранного предмета с экипированным
- Визуальное сравнение статистик:
  - Зеленый цвет — улучшение
  - Красный цвет — ухудшение
  - Белый цвет — без изменений
- Отображение описания предмета

**Триггеры:**
- Долгое нажатие на предмет в сетке
- Долгое нажатие на экипированный предмет

---

## 🗄️ STATE MANAGEMENT

### Zustand Store: `inventoryStore`
**Расположение:** `src/shared/stores/inventoryStore.ts`

**Состояние:**
```typescript
{
  items: Record<string, ItemState>           // Все предметы
  equipment: EquipmentSlots                  // Экипированные предметы
  containers: Record<string, InventoryContainer> // Контейнеры
  encumbrance: EncumbranceState              // Перегрузка
  playerStats: PlayerStatsSummary            // Статистики игрока
  selectedItemId: string | null              // Выбранный предмет
  searchQuery: string                        // Поисковый запрос
  activeFilter: InventoryFilter              // Активный фильтр
  questProtectedItemIds: Record<string, true> // Защищенные предметы
}
```

**Методы:**
- `equipItem(itemId, slotId)` — экипировка предмета
- `setQuickSlot(index, itemId)` — установка quick slot
- `moveItemWithinGrid(itemId, position)` — перемещение в сетке
- `selectItem(itemId)` — выбор предмета
- `setSearchQuery(query)` — установка поискового запроса
- `syncWithBackend(data)` — синхронизация с backend
- `getContainers()` — получение всех контейнеров

**Вычисляемые значения:**
- `encumbrance` — автоматически пересчитывается при изменении предметов
- `playerStats` — автоматически пересчитывается при изменении экипировки
- `containers` — автоматически создаются при экипировке предметов с `containerConfig`

---

## 🔌 BACKEND ИНТЕГРАЦИЯ

### Convex Queries & Mutations

**Query:**
- `api.inventory.get` — получение всех данных инвентаря
  - Возвращает: `{ items, containers, equipment }`

**Mutations:**
- `api.inventory.seedInventory` — создание стартового инвентаря
- `api.inventory.move` — перемещение предмета (TODO)
- `api.inventory.equip` — экипировка предмета (TODO)

**Синхронизация:**
- Автоматическая при загрузке страницы
- При изменении данных через mutations
- Двусторонняя синхронизация (client ↔ server)

---

## 🎨 UI/UX ОСОБЕННОСТИ

### Визуальный стиль:
- **Темная тема** (slate-950 фон)
- **Glass morphism** эффекты (`glass-panel`)
- **Цветовая кодировка редкости:**
  - Common — серый
  - Uncommon — зеленый
  - Rare — синий
  - Epic — фиолетовый
  - Legendary — янтарный

### Анимации:
- `AnimatedCard` — плавное появление карточек
- Hover эффекты на интерактивных элементах
- Drag overlay с превью предмета
- Transition эффекты при изменении состояния

### Адаптивность:
- Grid система (12 колонок)
- Responsive breakpoints
- Адаптивное растягивание компонентов

---

## 🔧 ВСПОМОГАТЕЛЬНЫЕ СИСТЕМЫ

### 1. Drag & Drop System
**Store:** `useInventoryDragStore`
**Расположение:** `src/features/inventory/model/useInventoryDrag.ts`

**Функциональность:**
- Отслеживание состояния перетаскивания
- Определение целевой зоны сброса
- Обработка событий pointer events

### 2. Quest Item Protection
**Hook:** `useQuestItemProtection`
**Расположение:** `src/features/inventory/model/useQuestItemProtection.ts`

**Функциональность:**
- Защита квестовых предметов от удаления
- Визуальная индикация (amber badge)
- Предотвращение случайной потери

### 3. Item Filtering
**Функция:** `filterItems`
**Расположение:** `src/features/inventory/model/utils.ts`

**Фильтры:**
- Поиск по названию
- Фильтр по типу (weapon, armor, consumable, etc.)
- Фильтр по редкости

---

## 📊 ПОТОК ДАННЫХ

```
┌─────────────┐
│   Convex    │
│   Backend   │
└──────┬──────┘
       │
       │ inventory.get()
       ▼
┌─────────────────┐
│ InventoryPage   │
│ (useQuery)      │
└──────┬──────────┘
       │
       │ syncWithBackend()
       ▼
┌─────────────────┐
│ inventoryStore  │
│ (Zustand)       │
└──────┬──────────┘
       │
       ├──► EquipmentSlots
       ├──► InventoryGrid
       ├──► ContainerPanel
       ├──► QuickStatsPanel
       └──► ItemDetailsPanel
```

---

## 🚀 БУДУЩИЕ УЛУЧШЕНИЯ

### Планируемые функции:
1. **Сортировка предметов** — по типу, весу, редкости
2. **Группировка предметов** — стеки однотипных предметов
3. **Автосортировка** — автоматическая организация инвентаря
4. **Фильтры по тегам** — расширенная фильтрация
5. **Поиск по статистикам** — поиск предметов с определенными статами
6. **История изменений** — отслеживание изменений инвентаря
7. **Экспорт/Импорт** — сохранение конфигурации инвентаря
8. **Горячие клавиши** — быстрые действия с клавиатуры
9. **Контекстное меню** — правый клик для действий
10. **Визуализация веса** — прогресс-бар перегрузки

### Технические улучшения:
1. **Виртуализация сетки** — для больших инвентарей
2. **Оптимизация рендеринга** — мемоизация компонентов
3. **Оффлайн режим** — работа без подключения
4. **Конфликт-резолюция** — обработка одновременных изменений
5. **Batch операции** — групповые действия с предметами

---

## 📝 ЗАМЕТКИ ПО РЕАЛИЗАЦИИ

### Сильные стороны:
✅ Модульная архитектура
✅ Четкое разделение ответственности
✅ Типобезопасность (TypeScript)
✅ Реактивность (Zustand)
✅ Синхронизация с backend

### Области для улучшения:
⚠️ Некоторые mutations еще не реализованы полностью
⚠️ Нет обработки ошибок синхронизации
⚠️ Можно добавить оптимистичные обновления
⚠️ Нет тестов для критических компонентов

---

## 🎯 ИСПОЛЬЗОВАНИЕ

### Типичный пользовательский сценарий:

1. **Открытие инвентаря** → Загрузка данных из Convex
2. **Просмотр предметов** → Сетка с визуализацией
3. **Выбор предмета** → Клик → Детальная панель
4. **Экипировка** → Drag & Drop или кнопка Equip
5. **Сравнение** → Долгое нажатие → Модальное окно
6. **Организация** → Перетаскивание в контейнеры
7. **Быстрый доступ** → Настройка quick slots

---

## 🔗 СВЯЗАННЫЕ КОМПОНЕНТЫ

- `ItemCard` — карточка предмета
- `ItemTooltip` — подсказка при hover
- `AnimatedCard` — анимированная карточка
- `Heading` — заголовки
- `MotionContainer` — контейнер с анимацией

---

*Документ обновлен: 2024*











