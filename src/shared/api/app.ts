/**
 * Backend API types for Eden treaty client.
 *
 * Generated via `npm run api:types` from the backend (`server/src/app.ts`) into `src/shared/api/__generated__/server`.
 * Frontend imports ONLY generated `.d.ts` files (no `server/src/*` imports).
 */

export type { App } from './__generated__/server/app'
