# Интеграция и Чек-лист разработки системы инвентаря

> **Статус**
> - ✅ Пересчёт статов игрока от экипировки подключён.
> - ✅ Mastery-карты подхватываются при экипировке оружия (InventoryStore → EquipmentStatsPanel).
> - ✅ Защита квестовых предметов в инвентаре (quest items помечены, drop/discard блокируются логически).
> - ⏳ Боевой хук в combatCards ещё впереди.

## 🔗 Интеграция с другими системами игры

### 1. Интеграция с Combat System

```typescript
// src/features/combatCards/lib/masteryIntegration.ts

import { useInventoryStore } from '@/shared/stores/inventoryStore';
import { useCombatStore } from '@/features/combat/model/store';

/**
 * При экипировке оружия, разблокировать соответствующие боевые карточки
 * в зависимости от уровня мастерства
 */
export function useMasteryCardUnlock() {
  const inventory = useInventoryStore();
  const combat = useCombatStore();

  const onEquipWeapon = (itemId: string) => {
    const item = inventory.items[itemId];
    const mastery = inventory.masteries[itemId];

    if (!mastery) return;

    // Получить карточки для текущего уровня мастерства
    const availableCards = mastery.unlockedCards.filter(
      (card) => card.requiredMasteryLevel <= mastery.level
    );

    // Добавить карточки в руку боя
    combat.addCardsToHand(availableCards);
  };

  return { onEquipWeapon };
}

// Пример: при использовании оружия, получаем XP мастерства
export function incrementMasteryXp(itemId: string, xpGain: number) {
  const store = useInventoryStore();
  const mastery = store.masteries[itemId];

  if (!mastery) return;

  const updated = {
    ...mastery,
    xp: mastery.xp + xpGain,
  };

  // Проверить левел-ап
  if (updated.xp >= updated.nextLevelXp) {
    updated.level += 1;
    updated.xp = 0;
    updated.nextLevelXp = getLevelUpThreshold(updated.level);
    // Уведомить игрока о новых разблокированных карточках
  }
}
```

### 2. Интеграция с Player Stats System

```typescript
// src/features/playerStats/lib/equipmentImpact.ts

/**
 * Пересчитать статистику персонажа в зависимости от экипировки
 */
export function calculatePlayerStatsFromEquipment(equipment: any) {
  const stats = {
    baseHealth: 100,
    baseArmor: 0,
    baseSpeed: 5,
    baseStamina: 100,
  };

  Object.entries(equipment).forEach(([slotId, item]) => {
    if (!item) return;

    // Броня дает защиту
    if (item.kind === 'armor') {
      stats.baseArmor += item.stats.defense || 0;
    }

    // Оружие влияет на урон (управляется Combat System)
    // Рюкзак увеличивает вместимость (управляется Inventory)
  });

  return stats;
}

// Hook для пересчета статистики при смене экипировки
export function useEquipmentStats() {
  const { equipment } = useInventoryStore();

  const stats = useMemo(
    () => calculatePlayerStatsFromEquipment(equipment),
    [equipment]
  );

  return stats;
}
```

### 3. Интеграция с Quest System

```typescript
// src/features/quests/lib/questItemProtection.ts

export const useQuestItemProtection = () => {
  const { quests } = useActiveQuests();
  const setQuestProtectedItems = useInventoryStore((state) => state.setQuestProtectedItems);
  const isQuestItem = useInventoryStore((state) => state.isQuestItem);

  const questItemIds = useMemo(() => {
    const ids = new Set<string>();
    quests.forEach((quest) => {
      const required = extractIds((quest as any)?.requiredItems);
      required.forEach((id) => ids.add(id));
    });
    return Array.from(ids);
  }, [quests]);

  useEffect(() => {
    setQuestProtectedItems(questItemIds);
  }, [questItemIds, setQuestProtectedItems]);

  const canDropItem = (itemId: string) => !isQuestItem(itemId);

  return { questItemIds, canDropItem };
};
```

### 4. Интеграция с Map System (Looting)

```typescript
// src/features/map/lib/lootIntegration.ts

/**
 * Когда игрок подберет предмет на карте, добавить в инвентарь
 */
export function useLootPickup() {
  const inventory = useInventoryStore();
  const outbox = useInventoryOutbox();

  const pickupItem = (item: any, location: { x: number; y: number }) => {
    // Проверить место в инвентаре
    const canFit = checkInventorySpace(inventory, item);

    if (!canFit) {
      // Показать сообщение: инвентарь переполнен
      return { success: false, reason: 'inventory_full' };
    }

    // Добавить предмет
    inventory.addItem(item, 1);

    // Записать в outbox для синхронизации
    outbox.enqueue({
      type: 'item_added',
      payload: {
        itemId: item.id,
        quantity: 1,
        location,
        source: 'loot',
      },
    });

    // Удалить со сцены / с карты
    mapStore.removeObject(item.id);

    return { success: true };
  };

  return { pickupItem };
}
```

---

## 📋 Чек-лист разработки

### Фаза 1: Базовая архитектура (1-2 дня)

- [ ] Создать структуру FSD директорий
- [ ] Определить все типы в `src/shared/types/item.ts`
- [ ] Создать Zustand store (`inventoryStore.ts`)
- [ ] Добавить Outbox store (`inventoryOutbox.ts`)
- [ ] Создать Convex схему и таблицы
- [ ] Написать базовые мутации Convex (`inventory:sync`)

### Фаза 2: UI компоненты (2-3 дня)

- [ ] Создать `InventoryPage.tsx` (главный экран)
- [ ] Создать `InventoryGrid.tsx` (сетка 10x6)
- [ ] Создать `ItemCard.tsx` (карточка с рарностью)
- [ ] Создать `EquipmentSlots.tsx` (слоты экипировки)
- [ ] Создать `DetailedTooltip.tsx` (информация о предмете)
- [ ] Создать `EncumbranceBar.tsx` (полоса нагрузки)
- [ ] Создать `QuickAccessBar.tsx` (быстрый доступ)
- [ ] Создать `ItemSearch.tsx` (поиск и фильтры)

### Фаза 3: Drag & Drop логика (2 дня)

- [ ] Реализовать `GridManager` для коллизий
- [ ] Создать `useDragDrop` hook
- [ ] Реализовать перетаскивание между слотами
- [ ] Добавить визуальный feedback (цветные границы, анимации)
- [ ] Реализовать вращение предмета (R ключ)
- [ ] Протестировать на нескольких предметах

### Фаза 4: Мобильная адаптация (1-2 дня)

- [ ] Создать `TouchGestureHandler` для жестов
- [ ] Реализовать долгое нажатие (long-press)
- [ ] Реализовать свайп между вкладками
- [ ] Адаптировать размеры под мобильные (≥60px)
- [ ] Протестировать на iOS и Android
- [ ] Добавить haptic feedback

### Фаза 5: Оффлайн-синхронизация (1 день)

- [ ] Реализовать Zustand persist middleware
- [ ] Создать `useSyncInventory` hook
- [ ] Обработать события online/offline
- [ ] Протестировать отправку очереди на сервер
- [ ] Добавить обработку конфликтов

### Фаза 6: Система ограничений (1 день)

- [ ] Реализовать расчет веса (`calculateTotalWeight`)
- [ ] Реализовать уровни нагрузки (`getEncumbranceLevel`)
- [ ] Применить штрафы к характеристикам
- [ ] Показать визуальный индикатор перегрузки
- [ ] Блокировать действия при immobile

### Фаза 7: Интеграция с игровыми системами (2-3 дня)

- [ ] Подключить к Combat System (разблокировка карточек)
- [ ] Подключить к Player Stats (влияние брони)
- [ ] Подключить к Quest System (защита квестовых предметов)
- [ ] Подключить к Map System (подбор лута)
- [ ] Протестировать синергию между системами

### Фаза 8: Полировка и оптимизация (1-2 дня)

- [ ] Добавить анимации (Framer Motion)
- [ ] Оптимизировать перерендеры (React.memo, useMemo)
- [ ] Добавить звуки (pickup, equip, error)
- [ ] Протестировать производительность
- [ ] Документировать API

### Фаза 9: Тестирование и исправление ошибок (1-2 дня)

- [ ] Юнит-тесты для `GridManager`
- [ ] Интеграционные тесты для Zustand + Convex
- [ ] E2E тесты для основных сценариев
- [ ] Регрессионное тестирование
- [ ] Фиксация найденных ошибок

---

## 🧪 Пример реализации одного предмета

### Тестовый сценарий: Добавление меча в инвентарь

```typescript
// Шаг 1: Определить предмет
const testSword: Item = {
  id: 'iron-sword-001',
  kind: 'weapon',
  name: 'Железный мечEOF',
  description: 'Крепкое оружие из обычного железа',
  icon: '/icons/weapons/iron-sword.png',
  rarity: 'uncommon',
  stats: {
    damage: 15,
    weight: 2.5,
    width: 1,
    height: 2,
    maxDurability: 100,
  },
  quantity: 1,
  lore: 'Простой и надежный меч, куть которого помечена мастером гильдии оружейников',
  tags: ['weapon', 'melee', 'common-materials'],
};

// Шаг 2: Добавить в инвентарь
const { addItem } = useInventory();
addItem(testSword);

// Шаг 3: Проверить в UI
// - Предмет появится в сетке инвентаря
// - Рамка зеленого цвета (uncommon)
// - Иконка видна
// - Можно перетащить

// Шаг 4: Надеть меч
const { equipItem } = useInventory();
equipItem('iron-sword-001', 'primary');

// Шаг 5: Проверить экипировку
// - Меч появится в слоте "primary"
// - Характеристики обновятся (урон +15)
// - Быстрое приращение XP мастерства при использовании

// Шаг 6: Проверить оффлайн-синхронизацию
// - Выключить сеть
// - Добавить еще один предмет
// - Включить сеть
// - Оба предмета должны синхронизироваться
```

---

## 📊 Метрики производительности

### Рекомендуемые KPI

```typescript
// Время загрузки инвентаря
const INVENTORY_LOAD_TIME_TARGET = 300; // мс

// Время перетаскивания предмета
const DRAG_RESPONSE_TIME_TARGET = 16; // мс (60fps)

// Память на хранение состояния
const MAX_STORE_SIZE = 5; // MB

// Синхронизация с сервером
const SYNC_TIMEOUT = 10000; // мс
const MAX_OUTBOX_SIZE = 100; // событий до предупреждения
```

---

## 🎯 Опциональные расширения (Phase 2+)

### 1. Улучшенный поиск с фильтрацией
- Фильтры по типу, редкости, весу
- Сохранение последних поисков
- История фильтров

### 2. Система торговли / продажи
- NPC торговцы с инвентарем
- Система цен и торга
- История транзакций

### 3. Крафтинг в инвентаре
- Рецепты для комбинирования предметов
- Визуальное отображение рецепта
- Прогресс крафтинга

### 4. Система разложения предметов
- Разложить броню на материалы
- Переплавить металл
- Получить ресурсы

### 5. Сортировка и организация
- Пользовательские вкладки
- Избранные предметы
- Метки и цветовые теги

### 6. Статистика инвентаря
- Общая стоимость
- Вес по категориям
- Процент заполнения
- График использования

### 7. Интеграция с социальными функциями
- Обмен предметами между игроками
- Система ярмарки
- Сравнение инвентарей

---

## 🚀 Советы по оптимизации

### React оптимизация

```typescript
// Использовать React.memo для ItemCard
export default React.memo(ItemCard, (prev, next) => {
  return (
    prev.item.id === next.item.id &&
    prev.isDragging === next.isDragging
  );
});

// Использовать useMemo для фильтрации
const filteredItems = useMemo(
  () => items.filter(item => item.kind === activeFilter),
  [items, activeFilter]
);

// Использовать useCallback для обработчиков
const handleDrop = useCallback((x, y) => {
  moveItem(draggedItem.id, 'main', { x, y });
}, [draggedItem, moveItem]);
```

### Zustand оптимизация

```typescript
// Выбирать только нужные части состояния
const items = useInventoryStore((state) => state.items);
const equipment = useInventoryStore((state) => state.equipment);

// Не выбирать все состояние
// const all = useInventoryStore(); // ❌ Плохо

// Использовать selectors
const selectItems = (state) => state.items;
const items = useInventoryStore(selectItems);
```

### Конвекс оптимизация

```typescript
// Индексировать поля в схеме
export default defineSchema({
  inventory: defineTable({
    playerId: v.id('players'),
    // ...
  }).index('by_playerId', ['playerId']), // ✅ Быстрые запросы
});

// Использовать пагинацию для больших инвентарей
const firstPage = await db.query('inventory').take(20);
```

---

## 📚 Полезные ресурсы

### Документация
- [Zustand Docs](https://docs.pmnd.rs/zustand/)
- [Framer Motion](https://www.framer.com/motion/)
- [Convex Docs](https://docs.convex.dev/)
- [TailwindCSS](https://tailwindcss.com/)

### Примеры из игр
- **Escape from Tarkov**: Система Tetris-стиля, вращение предметов, реалистичный вес
- **Disco Elysium**: Атмосферный интерфейс, текстовые описания, цветовая схема
- **Baldur's Gate 3**: Табы категорий, быстрый поиск, сравнение характеристик

### Инструменты
- React DevTools Profiler (для анализа производительности)
- Chrome DevTools (для анализа памяти)
- Zustand DevTools (для отладки состояния)
- Convex Dashboard (для мониторинга БД)

---

## 🔐 Безопасность

### Валидация на сервере

```typescript
// convex/inventory.ts

export const syncInventory = mutation({
  args: {
    deviceId: v.string(),
    playerId: v.id('players'),
    events: v.array(/* ... */),
  },
  handler: async (ctx, args) => {
    // ✅ Проверить, что playerId принадлежит текущему пользователю
    const user = await ctx.auth.getUserIdentity();
    if (!user) throw new Error('Not authenticated');

    const player = await ctx.db.get(args.playerId);
    if (player.userId !== user.sub) {
      throw new Error('Unauthorized');
    }

    // ✅ Валидировать события
    for (const event of args.events) {
      validateInventoryEvent(event);
    }

    // ✅ Применить события атомарно
    // ...
  },
});
```

### Защита от читов

- Валидировать вес предметов на сервере
- Проверять существование предметов в БД
- Логировать подозрительные события
- Использовать transaction для атомарности
