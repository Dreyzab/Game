# АНАЛИЗ СИСТЕМЫ АТРИБУТОВ И ХАРАКТЕРИСТИК

> **⚠️ КРИТИЧНО:** Этот документ описывает проблемы системы «Внутренний Парламент».  
> **Актуальная реализация:** 18 голосов/навыков в 6 группах, но в коде есть **2 несовместимых набора идентификаторов** (см. ниже).  
> **Статус:** Требуется унификация ID + миграция данных/контента.
>
> **Примечание о миграции:** Backend использует Bun + Elysia и находится в `server/src/`.

---

## 📋 ОБЗОР

Система атрибутов («голосов») — это набор внутренних голосов, влияющих на:
- проверки в визуальной новелле;
- «советы»/консультации от голосов;
- скейлинг боевых прототипов (Trinity Protocol);
- кооперативные роли.

Критическая проблема проекта сейчас не в количестве голосов (их 18), а в **несовпадении ключей**, по которым разные подсистемы читают уровни этих голосов.

---

## ⚠️ КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1) Несогласованность ID голосов/скилов (2 параллельные системы)

#### Система A — «каталог голосов» (canonical voice IDs)
Источник: `src/shared/types/parliament.ts`
- **18 голосов** в 6 группах (6 × 3)
- **ID голосов:**  
  `force`, `resilience`, `endurance`,  
  `perception`, `reaction`, `coordination`,  
  `logic`, `rhetoric`, `analysis`,  
  `authority`, `suggestion`, `courage`,  
  `gambling`, `drama`, `creativity`,  
  `empathy`, `solidarity`, `honor`
- **Группы:** `body`, `motorics`, `mind`, `consciousness`, `psyche`, `sociality`
- **Плюсы:** много метаданных (иконки/описания/боевые эффекты/комментарии), активно используется в UI и боевых прототипах.

#### Система B — «прогресс/скилы игрока» (persisted skill IDs)
Источники:
- стартовые значения: `server/src/lib/gameProgress.ts` (`STARTING_SKILLS`)
- типы VN-полифонии: `src/entities/visual-novel/model/types.ts` (`VoiceId`)
- хранение в БД: `server/src/db/schema/players.ts` (`game_progress.skills`)

- **18 навыков** в 6 группах, но **другие ключи для части BODY-группы**
- **ID навыков:**  
  `strength`, `endurance`, `stamina`,  
  `perception`, `reaction`, `coordination`,  
  `logic`, `rhetoric`, `analysis`,  
  `authority`, `suggestion`, `courage`,  
  `drama`, `creativity`, `gambling`,  
  `solidarity`, `honor`, `empathy`

#### Что ломается из‑за расхождения
- Компоненты, ожидающие `force/resilience`, читают `progress.skills` и получают **0** (потому что backend хранит `strength/stamina`).
- Контент/советы, написанные под «другие» ключи, становятся недостижимыми/невалидными.

---

### 2) Несогласованность стартовых значений

Сейчас стартовые значения задаются в **двух местах** разными ключами:

- `server/src/lib/gameProgress.ts` (`STARTING_SKILLS`) — создаёт прогресс игрока в `/vn/state` и влияет на проверки/советы:
  - BODY: `strength`, `endurance`, `stamina`
- `src/shared/types/parliament.ts` (`STARTING_VOICE_LEVELS`) — описывает старт «канонических» голосов:
  - BODY: `force`, `resilience`, `endurance`

Это не просто разный нейминг: в BODY‑группе сейчас два термина конкурируют за «третье место» ( `stamina` vs `resilience` ), поэтому при унификации придётся принять дизайнерское решение.

---

### 3) Неконсистентное использование в UI/фичах

Примеры, где несоответствие ключей видно прямо в коде:
- `src/entities/psyche/model/usePsyche.ts` и `src/entities/psyche/ui/PsychePanel.tsx`:
  - UI строится по `PARLIAMENT_VOICES` (ID из системы A),
  - уровни берутся из `progress.skills` (система B) → `force/resilience` в текущем прогрессе отсутствуют.
- `src/features/visual-novel/consultation/model/useConsultationMode.ts`:
  - уровень навыка берётся как `skills[characterId]`,
  - если `characterId` не совпадает с ключами из `game_progress.skills`, совет никогда не станет доступным.
- `src/shared/data/cards.ts` (Trinity Protocol):
  - `VoiceId` импортируется из `src/shared/types/parliament.ts`,
  - `generateDeckFromEquipment(..., voiceLevels, ...)` ожидает карту уровней по ключам системы A.

---

### 4) Легаси/экспериментальные ID в VN‑контенте

В сценариях встречаются проверки/советы с ID, которых **нет** ни в системе A, ни в системе B. В текущем состоянии такие проверки будут работать как «уровень 0» (и/или будут недостижимы при фильтрации).

Примеры:
- `src/entities/visual-novel/scenarios/prolog/scenarioTr-ST.ts`: `technophile`
- `src/entities/visual-novel/scenarios/chapter1/factions/synthesis_scenes.ts`: `reflexes`, `technophile`
- `src/entities/visual-novel/scenarios/chapter1/side_quests.ts`: `cynicism`
- `src/shared/data/visualNovel/testSceneWithAdvices.ts`: советы с `characterId: 'intuition' | 'cynicism'` (при текущем `skills` эти советы не появятся)

Отдельно: `whisper/glitch/...` — это **не голоса**, а `InjectionEffect` (визуальные эффекты) из `src/entities/visual-novel/model/types.ts`.

---

## 📊 ТЕКУЩЕЕ ИСПОЛЬЗОВАНИЕ (КАРТА СИСТЕМЫ)

### Хранение данных (backend)
- `server/src/db/schema/players.ts`: `game_progress.skills: Record<string, number>` (JSON)
- `server/src/api/routes/vn.ts`: создаёт/читает прогресс, применяет изменения при `/vn/commit`

### UI персонажа
- `src/pages/CharacterPage.tsx`: отображает скиллы из `progress.skills` (ключи системы B: `strength/...`)

### UI «Психики»
- `src/entities/psyche/ui/PsychePanel.tsx`: рендерит голоса системы A (`force/...`) и читает уровни из `progress.skills` (система B)

### Консультации (советы голосов)
- `src/features/visual-novel/consultation/lib/voiceDefinitions.ts`: UI‑метаданные из системы A
- `src/features/visual-novel/consultation/lib/consultationUtils.ts`: доступность советов по `skills[characterId]`

### Бой / Trinity Protocol (прототип)
- `src/shared/data/cards.ts`: синтез карт и генерация колоды завязаны на `VoiceId` из системы A

---

## 📈 РЕКОМЕНДАЦИИ ПО ИСПРАВЛЕНИЮ

### Шаг 1: Выбрать единый набор ID
Варианты:
1) **Принять систему A (`force/resilience/endurance`) как единую** и мигрировать прогресс/VN‑контент (рекомендуется, т.к. A уже «каталог» и используется в боевых прототипах/кооп‑ролях/иконках).
2) Принять систему B (`strength/endurance/stamina`) как единую и переименовать `parliament.ts` и всё, что на нём основано.
3) Временно добавить слой маппинга (быстрый фикс), но всё равно запланировать миграцию на один набор.

### Шаг 2: Сделать явный маппинг (минимум)
Ввести единый слой соответствий и применять его при чтении/записи:
- `strength` ↔ `force`
- `stamina` ↔ `resilience` (**нужно дизайнерское решение по терминам/смыслу**)
- остальные ключи совпадают

### Шаг 3: Починить самые заметные точки
- `usePsyche.getVoiceLevel(...)`: либо читать из маппинга, либо перейти на единый набор ID.
- `consultationUtils`: гарантировать, что `characterId` совпадает с ключом навыка (или маппится).
- `cards.ts`: подавать `voiceLevels` в ожидаемом формате (или унифицировать ключи).

### Шаг 4: Привести контент VN к допустимым ID
- заменить/удалить `technophile/reflexes/cynicism/intuition/...` либо добавить их в единую систему (если они реально нужны).

### Шаг 5: Усилить типизацию
- ужесточить `VisualNovelChoiceRequirement.skillCheck.skill` и `VisualNovelAdvice.characterId` (сейчас это `string`), чтобы ловить рассинхрон на этапе компиляции.

---

## 📝 ТАБЛИЦА СООТВЕТСТВИЙ (A ↔ B)

| Система A (`parliament.ts`) | Система B (`game_progress.skills`) | Статус |
|---|---|---|
| `force` | `strength` | ⚠️ разные ключи |
| `resilience` | `stamina` | ⚠️ разные ключи + спор по смыслу |
| `endurance` | `endurance` | ✅ совпадает |
| `perception` | `perception` | ✅ совпадает |
| `reaction` | `reaction` | ✅ совпадает |
| `coordination` | `coordination` | ✅ совпадает |
| `logic` | `logic` | ✅ совпадает |
| `rhetoric` | `rhetoric` | ✅ совпадает |
| `analysis` | `analysis` | ✅ совпадает |
| `authority` | `authority` | ✅ совпадает |
| `suggestion` | `suggestion` | ✅ совпадает |
| `courage` | `courage` | ✅ совпадает |
| `gambling` | `gambling` | ✅ совпадает |
| `drama` | `drama` | ✅ совпадает |
| `creativity` | `creativity` | ✅ совпадает |
| `empathy` | `empathy` | ✅ совпадает |
| `solidarity` | `solidarity` | ✅ совпадает |
| `honor` | `honor` | ✅ совпадает |

---

## 🎯 ПРИОРИТЕТЫ

1) **КРИТИЧНО:** выбрать один набор ID и зафиксировать маппинг/миграцию
2) **ВЫСОКО:** привести `usePsyche` и консультации к тем же ключам, что и прогресс
3) **ВЫСОКО:** почистить VN‑контент от невалидных ID
4) **СРЕДНЕ:** усилить типизацию skill IDs в `shared/types/visualNovel.ts`

---

## 📚 ССЫЛКИ НА ФАЙЛЫ

### Определения и хранение
- `src/shared/types/parliament.ts` — каталог голосов (система A)
- `server/src/lib/gameProgress.ts` — стартовые навыки (система B)
- `server/src/db/schema/players.ts` — хранение `game_progress.skills`

### Использование в UI/логике
- `src/pages/CharacterPage.tsx` — UI персонажа (читает систему B)
- `src/entities/psyche/model/usePsyche.ts` — читает A, уровни берёт из B (рассинхрон)
- `src/features/visual-novel/consultation/lib/consultationUtils.ts` — фильтрация советов по `skills[characterId]`
- `src/shared/data/cards.ts` — Trinity Protocol (ожидает систему A)

### Проблемный контент (примеры)
- `src/entities/visual-novel/scenarios/prolog/scenarioTr-ST.ts`
- `src/entities/visual-novel/scenarios/chapter1/factions/synthesis_scenes.ts`
- `src/entities/visual-novel/scenarios/chapter1/side_quests.ts`
- `src/shared/data/visualNovel/testSceneWithAdvices.ts`
