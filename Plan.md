# План перезапуска проекта: новая архитектура Bun + React

Этот документ является генеральным планом перехода `grezwanderer3` на современный стек и монорепозиторную архитектуру.

## 🎯 Цель
Перейти от текущей реализации к новой архитектуре Bun + React, сфокусировавшись на **Детективном режиме (Detective Mode)** как на первой и основной части перезапуска. Остальные системы будут портированы позже.

## �️ Технологический стек и монорепозиторий

- **Runtime**: **Bun 1.x** (единый рантайм, пакетный менеджер и бандлер).
- **Frontend**: **React 19** + **Vite** + **Tailwind CSS v4**.
- **Backend**: **ElysiaJS** + **bun:sqlite** + **Redis** (Pub/Sub).
- **Архитектура**: **Monorepo (Bun Workspaces)**. Структура `apps/*` (фронт/бэк) и `packages/*` (shared logic). Это устраняет необходимость в скриптах вроде `copy-shared.js`, так как бэкенд и фронтенд будут использовать один и тот же код из `packages/shared`.

## �️ Система логирования и Тестирования

Для обеспечения стабильности и быстрой отладки в новой архитектуре внедряется единый стандарт:

### Логирование (Centralized Logger)
Мы создаем `@repo/shared/lib/logger.ts`, который:
- **Унифицирует вывод**: Заменяет разрозненные `console.log` и `fetch` на структурированные методы (`logger.info`, `logger.error`).
- **Контекст**: Автоматически прикрепляет Metadata (DeviceID, UserID, SessionID).
- **Transport**: В DEV выводит в консоль, в PROD может отправлять логи на удаленный Ingestion Server (как это было реализовано через `fetch` в текущем проекте).

- **API (Eden Treaty)**: Типизированное тестирование эндпоинтов сервера без необходимости запускать полный сетевой стек.

## 📦 Управление Ассетами и Ошибками

### Ассеты (Diegetic UI)
- **Хранение**: `apps/web/public` для статики.
- **Конфигурация**: Реестр ассетов в `@repo/shared/data` (связь ID с путями).

### Обработка ошибок
- **Zod Validation**: Валидация всех входов/выходов API.
- **Global Error Boundary**: Изоляция критических ошибок UI.
- **Toast System**: Централизованные уведомления.

## 🌍 Локализация (i18n)
- **Централизация**: Переводы (`ru`, `en`, `de`) в `packages/shared/locales`.
- **Синхронность**: Единые ключи для сервера и клиента.

## 📡 Сетевая устойчивость (Resilience)
- **Reconnection logic**: Авто-переподключение WebSockets в Survival Mode.
- **State Reconciliation**: Сверка состояния при восстановлении связи.

## 🚀 Развертывание и CI/CD
- **Docker**: Оптимизированные образы на базе `oven/bun`.
- **CI/CD**: GitHub Actions для проверки типов, линтинга и тестов.

## 🛠️ Developer Experience (DevX)
- **Root Scripts**: Запуск всего стека одной командой (`bun dev`).
- **Path Aliases**: Настройка `@repo/*` для мгновенной навигации.

## �📁 Feature-Sliced Design (FSD)
Мы строго придерживаемся методологии FSD (`entities`, `features`, `widgets`, `pages`, `shared`, `app`, `processes`).
- **Изоляция**: Каждая фича независима.
- **Инструментарий**: Использование `eslint-plugin-boundaries` для контроля импортов между слоями.
- **Единый источник истины**: Устранение дублирования состояний (например, объединение `selectedPoint` и `selectedPointId`).

## 🗺️ Дорожная карта (Checklist)

### Фаза 1: Базовая инфраструктура
- [ ] **Настройка монорепозитория**
    - [ ] `bun init -y` в корне.
    - [ ] Конфигурация `workspaces` в `package.json`.
    - [ ] Настройка `tsconfig.json` с алиасами для `@repo/shared`.
- [ ] **Shared Package**
    - [ ] Создание `packages/shared` для Zod-схем, типов и констант.
    - [ ] **Logger Implementation**: Создание единого логгера в `packages/shared/lib/logger.ts`.
    - [ ] **Localization**: Централизация словарей переводов.
    - [ ] Вынос общих данных (информация о голосах, системы XP, ассеты).
- [ ] **Ops & Config**
    - [ ] Создание Docker-файлов и CI конфигурации.
    - [ ] Подготовка `.env.example` (Clerk, Mapbox, Redis).

### Фаза 2: Бэкенд (Apps/Server)
**Цель**: Создание чистого бэкенда на ElysiaJS + bun:sqlite + Drizzle ORM с типизированным API через Eden Treaty.

#### 1. Инфраструктура
- [ ] **apps/server/package.json**: Инициализация проекта. Зависимости: `elysia`, `drizzle-orm`, `bun-sqlite`, `cors`, `@repo/shared` (workspace).
- [ ] **apps/server/tsconfig.json**: Настройка путей и расширение корневого конфига.

#### 2. Слой данных (Database)
- [ ] **apps/server/src/db/schema.ts**:
    - [ ] Таблицы `players` и `game_progress` (базовый профиль).
    - [ ] **Detective-specific**: Таблицы `detective_hardlinks` и `detective_scans`.
    - [ ] (Опционально) Заглушки для `scenarios` и `map_points`.
- [ ] **apps/server/src/db/index.ts**: Инициализация экземпляра `bun:sqlite` и Drizzle.

#### 3. API Layer & Routes
- [ ] **apps/server/src/index.ts**: Основная точка входа. Настройка CORS и экспорта типов для Eden.
- [ ] **Реализация роутов (Detective Focus)**:
    - [ ] `POST /detective/scan` (валидация QR и возврат экшенов).
    - [ ] `GET /detective/state` (получение досье).
    - [ ] `PUT /detective/state` (синхронизация досье с защитой от конфликтов).
- [ ] **Auth Middleware**: Базовая проверка Clerk токенов или Guest ID.

#### 4. Верификация
- [ ] Запуск сервера через `bun run dev`.
- [ ] Проверка создания файла `sqlite.db` и инициализации схем.

### Фаза 3: Фронтенд (Apps/Web)
- [ ] **Инициализация Vite**
    - [ ] `bun create vite apps/web --template react-ts`.
    - [ ] Обновление до React 19, подключение **TanStack Query** и **Zustand**.
- [ ] **Слои FSD**
    - [ ] Создание структуры папок `src/app`, `pages`, `widgets`, `features`, `entities`, `shared`.
- [ ] **Стилизация**
    - [ ] Настройка **Tailwind CSS v4`.
    - [ ] Перенос UI-кита (Button, Badge, Glassmorphism эффекты).

### Фаза 4: Миграция функционала (Только Детектив)
- [ ] **Domain Migration**
    - [ ] **Schema Adaptation**: Перенос `detective_hardlinks` и `detective_scans` в SQLite.
    - [ ] **Dossier Logic**: Перенос `detectiveState` из `game_progress` в отдельный или упрощенный формат.
    - [ ] **QR Scanning**: Реализация серверной валидации хардлинков (из `server/src/api/routes/detective.ts`).
    - [ ] **UI Porting**: Перенос детекторных виджетов и хуков синхронизации.

### Фаза 5: Тестирование и запуск
- [ ] **Unit-тесты**: Покрытие критической логики через `bun test`.
- [ ] **Интеграция**: Проверка связки Web -> Server через Eden.
- [ ] **Производительность**: Проверка HMR и скорости запросов к SQLite.

---

> [!NOTE]
> Переход наSQLite облегчит локальную разработку, но мы сохраняем возможность возврата к Postgres в будущем за счет использования Drizzle ORM.
