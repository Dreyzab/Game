# Реализация карты Mapbox GL JS

## ✅ Что было реализовано

### 1. Базовая инфраструктура
- ✅ **MapboxMap** (`src/shared/ui/MapboxMap.tsx`) - базовая обёртка для Mapbox GL JS
  - Инициализация карты с токеном из `.env.local`
  - Автоматический fallback на Carto Dark Matter при отсутствии токена
  - Контролы навигации, геолокации и масштаба
  - Обработка ошибок загрузки стиля

### 2. Типы данных
- ✅ **Типы карты** (`src/shared/types/map.ts`)
  - `MapPoint` - точки интереса
  - `SafeZone` - безопасные зоны
  - `MapPointType`, `MapPointStatus`, `DangerLevel` и другие
  - Полная типизация для TypeScript

### 3. Компоненты точек
- ✅ **MapPointMarker** (`src/entities/map-point/ui/MapPointMarker.tsx`)
  - Визуальное отображение точек на карте
  - Разные цвета и иконки для разных типов точек
  - Индикаторы опасности и QR-кодов
  - Пульсирующая анимация для активных точек

- ✅ **MapPointPopup** (`src/entities/map-point/ui/MapPointPopup.tsx`)
  - Детальная информация о точке
  - Статус открытия (не найдено/обнаружено/исследовано)
  - Уровень опасности, фракция, расстояние
  - Кнопки действий (навигация, взаимодействие, QR-сканирование)

### 4. Управление зонами
- ✅ **SafeZonesControl** (`src/widgets/map/map-view/SafeZonesControl.ts`)
  - Управление GeoJSON слоями безопасных зон
  - Слои заливки, границ и лейблов
  - Динамическое обновление данных без пересоздания карты
  - Переключение видимости

### 5. Главный компонент
- ✅ **MapView** (`src/widgets/map/map-view/MapView.tsx`)
  - Интеграция всех компонентов карты
  - Управление маркерами и попапами
  - Обработка выбора точек
  - Кнопка центрирования на пользователе
  - Индикаторы загрузки

### 6. Хуки для данных
- ✅ **useMapData** (`src/shared/hooks/useMapData.ts`)
  - `useVisibleMapPoints` - загрузка точек карты из Convex
  - `useSafeZones` - загрузка безопасных зон
  - `useGeolocation` - получение геолокации пользователя
  - `useCenterOnUser` - центрирование карты на пользователе
  - Утилиты для работы с расстояниями и bbox

### 7. Интеграция страницы
- ✅ **MapPage** (`src/pages/MapPage.tsx`)
  - Полная страница карты с заголовком
  - Переключатель безопасных зон
  - Информационная панель для выбранной точки
  - Адаптивная вёрстка

### 8. Стили
- ✅ Импорт Mapbox GL CSS в `src/index.css`
- ✅ Кастомные стили для попапов и контролов

## 🚀 Как использовать

### 1. Настройка токена Mapbox

В файле `.env.local` добавьте:

```env
VITE_MAPBOX_TOKEN=pk.your_mapbox_token_here
```

Получите токен на: https://account.mapbox.com/access-tokens/

### 2. Запуск проекта

```bash
npm run dev
```

Перейдите на страницу `/map` в браузере.

### 3. Использование карты

Карта автоматически:
- ✅ Загружает точки из Convex (или использует демо-данные)
- ✅ Отображает маркеры с разными цветами для разных типов
- ✅ Показывает безопасные зоны зелёным цветом
- ✅ Позволяет кликать на точки для просмотра деталей
- ✅ Поддерживает геолокацию пользователя

## 📦 Структура компонентов

```
src/
├── shared/
│   ├── ui/
│   │   └── MapboxMap.tsx          # Базовая обёртка карты
│   ├── types/
│   │   └── map.ts                  # Типы данных карты
│   ├── hooks/
│   │   └── useMapData.ts           # Хуки для работы с данными
│   └── api/
│       └── convex.ts               # API интеграция (обновлено)
├── entities/
│   └── map-point/
│       └── ui/
│           ├── MapPointMarker.tsx  # Компонент маркера
│           ├── MapPointPopup.tsx   # Компонент попапа
│           └── index.ts
├── widgets/
│   └── map/
│       └── map-view/
│           ├── MapView.tsx         # Главный компонент карты
│           ├── SafeZonesControl.ts # Контрол безопасных зон
│           └── index.ts
└── pages/
    └── MapPage.tsx                 # Страница карты
```

## 🎨 Функциональность

### Типы точек и их цвета

| Тип | Иконка | Цвет | Описание |
|-----|--------|------|----------|
| `poi` | 📍 MapPin | Синий | Точка интереса |
| `npc` | 👥 Users | Зелёный | Персонаж |
| `quest` | 📜 Scroll | Жёлтый | Квест |
| `location` | 🏠 Home | Фиолетовый | Локация |
| `board` | 💬 MessageSquare | Оранжевый | Доска объявлений |
| `settlement` | 🚩 Flag | Голубой | Поселение |
| `anomaly` | ⚡ Zap | Красный | Аномалия |

### Статусы точек

- **not_found** (не найдено) - приглушенный цвет
- **discovered** (обнаружено) - яркий цвет
- **researched** (исследовано) - серый цвет

### Индикаторы

- 🔴 **Красная точка** - высокая опасность
- 🟠 **Оранжевая точка** - средняя опасность
- 🟣 **Фиолетовая точка** - требуется QR-код

### Безопасные зоны

- 🟢 Зелёный полигон с полупрозрачной заливкой
- Название зоны отображается в центре
- Можно включать/выключать через чекбокс на странице

## 🔧 Интеграция с Convex

Карта использует следующие Convex queries:

1. **mapPoints.listVisible** - получение видимых точек карты
   ```typescript
   {
     deviceId?: string
     userId?: string
     bbox?: { minLat, maxLat, minLng, maxLng }
     phase?: number
     limit?: number
   }
   ```

2. **zones.listSafeZones** - получение безопасных зон
   ```typescript
   {
     bbox?: { minLat, maxLat, minLng, maxLng }
   }
   ```

При отсутствии Convex используются демо-данные (4 точки возле Фрайбурга).

## 🎯 Демо-данные

Если Convex не настроен, карта показывает:
- Информационное бюро (poi)
- Безопасная зона (location)
- Рыночная площадь (settlement)
- Аномальная зона (anomaly)
- Одна безопасная зона (полигон)

Все точки расположены возле Фрайбурга, Германия (7.85, 48.0).

## 🐛 Отладка

### Проблема: Карта не загружается
- ✅ Проверьте `VITE_MAPBOX_TOKEN` в `.env.local`
- ✅ Если токена нет - автоматически используется Carto (не требует токена)
- ✅ Смотрите консоль браузера на ошибки

### Проблема: Точки не отображаются
- ✅ Проверьте, что Convex настроен (`VITE_CONVEX_URL`)
- ✅ Если Convex не настроен - используются демо-данные
- ✅ Откройте DevTools и проверьте Network запросы

### Проблема: Геолокация не работает
- ✅ Разрешите доступ к геолокации в браузере
- ✅ Используйте HTTPS (или localhost для разработки)

## 📚 Дополнительные ресурсы

- [Mapbox GL JS Docs](https://docs.mapbox.com/mapbox-gl-js/guides/)
- [MAP_GUIDE.md](./MAP_GUIDE.md) - полное руководство по карте
- [CONVEX_GUIDE.md](./CONVEX_GUIDE.md) - руководство по Convex

## ✨ Что дальше?

Возможные улучшения:
- Фильтры точек по типу/фракции
- Список точек в боковой панели
- Маршруты навигации до точек
- Кластеризация маркеров при большом зуме
- Режим AR для QR-сканирования
- Анимация движения врагов
- Зоны опасности (красные полигоны)

---

**Статус**: ✅ Полностью реализовано и готово к использованию!

